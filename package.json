{
  "name": "fly-varnish",
  "version": "1.0.0",
  "main": "server.js",
  "license": "MIT",
  "scripts": {
    "docker:build": "docker build -t fly-varnish .",
    "docker:test": "docker run -p 8080:8080 fly-varnish"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
